{
  // api_key: "",  // specified via environment variable
  // NOTE: The key defined as Circle CI env variable is available in qatesting
  scalyr_server: "app-qatesting.scalyr.com",
  debug_level: "5",

  server_attributes: {
     serverHost: "ci-codespeed-benchmarks-single-50mb-access-log-agent",
     tier: "benchmarks"
  }

  logs: [
     { path: "/tmp/access_log_50_mb.log", attributes: { parser: "accessLog" } }
  ],

  monitors: [
  ]
}
