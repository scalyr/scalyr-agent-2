{
  api_key: {API_KEY},
  scalyr_server: {SCALYR_SERVER},
  server_attributes: {
      serverHost: {SERVER_HOST},
  }
  verify_server_certificate: false,
  debug_level: "5",
  logs: [
     {
         path: "/root/scalyr-agent-dev/log/test_log*.log",
         attributes: { parser: "accessLog" },
     }
  ],

  monitors: [
  ]
}
