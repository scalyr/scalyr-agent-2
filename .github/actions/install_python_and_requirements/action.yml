name: "Install Python and requirements"
description: "Install Python interpreter and requirement libraries for the agent"

inputs:
  python_version:
    description: "Version of Python"

runs:
  using: "composite"
  steps:
    - name: Install python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

#    - name: Get pip cache dir
#      id: pip-cache
#      run: |
#        echo "::set-output name=dir::$(pip cache dir)"
#
#    - name: pip cache
#      uses: actions/cache@v2
#      with:
#        path: ${{ steps.pip-cache.outputs.dir }}
#        key: ${{ runner.os }}-pip-${{ hashFiles('dev-requirements-new.txt') }}
#        restore-keys: |
#          ${{ runner.os }}-pip-

    - name: Create venv
      shell: bash
      working-directory: ${{ github.workspace }}
      run: |
        python3 -m venv venv
        echo "PATH=$(pwd):${PATH}" >> "$GITHUB_ENV"
        venv/bin/python3 -m pip install -r dev-requirements-new.txt
